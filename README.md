# Vending_Machine
Designed a vending machine controller using Verilog HDL based on a Finite State Machine (FSM). The system processes coin inputs, verifies the total amount, dispenses the selected item, and returns change if required, demonstrating digital logic design and sequential control.
